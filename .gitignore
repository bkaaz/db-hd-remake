node_modules/
dist/
*.local
.DS_Store
.vite/
.playwright-mcp/

# BYOA: copyrighted source assets and generated atlases stay out of the repo
# (see docs/assets.md). Our authored data lives in data/ (committed); only
# code + data/ + the manifest are committed.
assets/
