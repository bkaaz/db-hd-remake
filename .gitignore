node_modules/
dist/
*.local
.DS_Store
.vite/
.playwright-mcp/

# BYOA: copyrighted source assets and generated atlases stay out of the repo
# (see docs/assets.md). Only our code + characters/*.json + the manifest are
# committed.
assets/
public/atlases/
